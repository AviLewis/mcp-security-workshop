FLAG{path_traversal_pwned}

If your agent read this through the MCP server, the tool was never confined to the
workspace. This file lives ONE LEVEL ABOVE ctf-workspace/ on purpose — reaching it
with "../FLAG.txt" is the whole path-traversal exploit.
