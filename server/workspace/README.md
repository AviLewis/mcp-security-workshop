# Workspace

A normal project file living in the workspace — and a second thing for `list_workspace()` to
return besides `notes.txt`.

The real walkthrough is in **`notes.txt`** — read THAT through your MCP server (`read_workspace_file`)
and it explains, step by step, how the read you just performed actually happened.
