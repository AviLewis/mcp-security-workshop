# Task 2 Workspace

A "normal" file your partner's agent is *meant* to read.

The lesson lands when, right after this, the same agent reads `../fake.env` —
a file it was never meant to see. Same tool, no extra permission, no warning.
That gap is the security boundary Task 3 attacks.
